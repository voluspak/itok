import { BookmarkIcon } from './Icons'
import InteractionButton from './InteractionButton'

const BookmarkControl: React.FC = () => {
  return (
    <InteractionButton>
      <BookmarkIcon />
    </InteractionButton>
  )
}

export default BookmarkControl
